<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Atividade</title>

    <style>
        .link-voltar {
            position: absolute;
            top: 40px;
            right: 40px;
        }

        body {
            padding: 20px 30px;
        }
    </style>
</head>
<body>
    <a href="<%= root_path %>" class="link-voltar">Voltar</a>

    <h1>Ação: <%= @atividade.acao_extensao %></h1>
    <p>Tipo: <%= @atividade.tipo || '-' %></p>
    <p>Carga horária: <%= @atividade.horas %>h</p>
    <p>Data de submissão: <%= @atividade.data_submissao.strftime("%d/%m/%Y") %></p>
    <p>Situação: <%= @atividade.situacao %></p>
    <p>Data de aprovação: <%= @atividade.data_aprovacao ? @atividade.data_aprovacao.strftime("%d/%m/%Y") : '-' %></p>

    <% if @atividade.anexo.attached? %>
        <p>Anexo: <%= link_to 'Baixar anexo', rails_blob_path(@atividade.anexo, disposition: 'attachment') %></p>
    <% end %>
</body>
</html>